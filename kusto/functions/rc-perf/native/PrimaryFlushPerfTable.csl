.create-or-alter function with (docstring = "[t:string, paritionId:string]
        where t=table name, paritionId=Parition Id.
        Return primary flush performance table.", folder = "rc-perf/native")
    PrimaryFlushPerfTable(t:string, partitionId:string) {
    let primaryNodeName = GetPrimaryNodeName(t, partitionId);
    FlushPerfTable(t, partitionId, primaryNodeName)
}
