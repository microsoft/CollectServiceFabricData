.create-or-alter function with (docstring = "function to create ??? TableView view for sftable",folder = "sftable") 
    TableView???(T:string) {
        TableView(T)
        | where RelativeUri endswith "???"
        // todo fill in fields
        | project Timestamp, 
            PartitionKey, 
            RowKey
        | order by Timestamp asc
    }
